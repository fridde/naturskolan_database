{% set q_id = 'card-body-' ~ random('abcdefghijklmnopqrstuvwxyz') ~ random(9999) %}


<div class="card">
    <div class="card-header">
        <h1 class="mb-0">
            <button class="btn btn-link" data-toggle="collapse" data-target="#{{ q_id }}">
                {% block q_title -%}{%- endblock %}
            </button>
        </h1>
    </div>
    <div id="{{q_id }}" class="collapse collapsed" data-parent="#accordion">
        <div class="card-body">
            {% block q_answer -%}{%- endblock %}
        </div>
    </div>
</div>
