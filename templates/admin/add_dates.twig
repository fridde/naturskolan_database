{%- extends "admin/admin_area.twig" -%}

{%- set topics = DATA.topics -%}

{% block content %}
    <a class="btn btn-primary" data-toggle="collapse" href="#help_content" role="button">
        Help
    </a>
    <div class="collapse" id="help_content">
        <div class="card card-body">
            <p>Values schould be given in format <b>topic_id: date</b> with visits sperated by <b>;</b> or a new line.
                The date should be given in the format YYYY-MM-DD and the topic_id can be found in the id column of the<a
                    href="table/topic">topic table</a>.<br>
              Alternatively, you can choose a certain topic and use the format <b>date</b> instead, without the topic_id.
        </div>
    </div>
    <div>
        <select class="date-lines">
            <option value="multiple">Flera teman samtidigt</option>
            {% for topic in topics %}
                <option value="{{ topic.id }}">{{ topic.label }}</option>
            {% endfor %}
        </select>
        <div class="add-dates">
            <button id="clean">Clean up</button>
            <button id="add">Add to database</button>
        </div>
    </div>
    <textarea class="date-lines" cols="20" rows="50"></textarea>
{% endblock %}

