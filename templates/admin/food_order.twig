{%- extends "admin/admin_area.twig" -%}{%- set calendar, date_strings = DATA.calendar, DATA.date_strings -%}{% block content %}    <div>        <button data-target="area-to-copy" class="copy-to-clipboard">Kopiera text</button>        <button id="order-food-mail">            <a href="mailto: {{- DATA.food_order_mail -}} ?subject=Matbeställning%20för%20Naturskolan&body=Hej!">Skicka matbeställningsmejl</a>        </button>    </div>    <div><textarea id="area-to-copy" cols="100" rows="50">{% for week_nr, days in calendar %}#########Vecka {{ week_nr }}#########{% for day_index, visits in days %}-------------# {{ date_strings[day_index] }}-------{% for visit in visits %}- {{ visit.group_name|default('???') }} från {{ visit.school_name }}. Temadag "{{ visit.topic_name }}", {{ visit.location }}. {{ visit.students_nr|default('???') }} elever.{% if visit.food_info_needed -%}{{ '  ' }}Specialkost: {{ visit.diet|default('???') }}{% endif %}{% endfor -%}{%- endfor -%}{%- endfor -%}</textarea></div>{% endblock %}