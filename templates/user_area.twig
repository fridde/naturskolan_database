{% import "macros.twig" as F %}

<!DOCTYPE html>
<html>
<head>
{{ include('head.twig')  }}
</head>
<body>
    <div class="container-fluid">
        {{ include('nav.twig') }}
        {%- block school_area -%} {%- endblock -%}
        {%- block password_modal -%} {%- endblock -%}
    </div>
</body>
</html>
