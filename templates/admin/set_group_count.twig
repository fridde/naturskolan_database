{%- extends "admin/admin_area.twig" -%}




{% block content %}
    <h1>Update or set group counts for schools</h1>
    <a class="btn btn-primary" data-toggle="collapse" href="#help_content" role="button">
        Help
    </a>
    <div class="collapse" id="help_content">
        <div class="card card-body">
            <p>Values schould be given in format <b>school_id, segment_id, count</b> with one value for each line. For example, to update S:t Persskolan's number of groups for årskurs 2/3 in 2019 to 5 groups, choose first "2019" in the selector and write <b>pers,2,5</b> into the text field.<br>Spaces and empty lines are allowed.</p>
        </div>
    </div>
    <div class="set-group-count">
        <button id="clean">Clean up</button>
        <button id="update">Update these values in the database</button>
    </div>
    <div>
        <select name="start-year" id="start-year-selector">
            <option>{{ "-1 year"|date("Y") }}</option>
            <option selected>{{ "now"|date("Y") }}</option>
            <option>{{ "+1 year"|date("Y") }}</option>
        </select>
    </div>
    <textarea id="group-count-lines" cols="20" rows="50"></textarea>
{% endblock %}



