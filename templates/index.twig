{%- extends 'base.twig' -%}

{%- set schools = getAllSchoolUrls(['anna', 'natu']) -%}

{%- block toolbar -%}{%- endblock -%}
{%- block content -%}
    <div class="row">
        <div class="col-md-3"></div>
        <div class="col-md-6">
            <h1 class="center">Välj skola</h1>
            <div class="flexbox">
                {% for school in schools %}
                    <a href="{{ school.url }}">
                        <button type="button" class="btn btn-default school-choice">{{ school.label }}</button>
                    </a>
                {% endfor %}
            </div>
        </div>
        <div class="col-md-3"></div>
    </div>



{%- endblock -%}
