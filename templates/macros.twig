{% macro atts(attributes) %}
    {% for key, val in attributes %}
        {%- if  key is integer -%}
            {{- val ~ " " -}}
        {%- else -%}
            {{ key }}="{{ val }}"{{" "}}
        {%- endif -%}
     {% endfor %}
 {% endmacro %}


{% macro a(address, content, attributes = []) %}
<a href="{{address}}" {% for key, val in attributes %}{{key}}="{{val}}" {% endfor %} >{{content|default(address)}}</a>
{% endmacro %}

{% macro select(options, label = "", attributes = []) %}

{% endmacro %}
