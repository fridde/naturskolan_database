{%- extends "admin/admin_area.twig" -%}{%- set calendar, date_strings = DATA.calendar, DATA.date_strings -%}{% block content %}    <div>        <button class="copy-to-clipboard">Kopiera text</button>        <button id="order-food-mail">            <a href="mailto: {{- DATA.food_order_mail -}} ?subject=Matbeställning%20för%20Naturskolan&body=Hej!">Skicka matbeställningsmejl</a>        </button>    </div>    <div><textarea class="area-to-copy" cols="100" rows="50">{% for week_nr, days in calendar %}#########Vecka {{ week_nr }}#########{% for day_index, visits in days %}-------------# {{ date_strings[day_index] }}-------{% for visit in visits %}- {{ visit.group_name|default('???') }}, {{ visit.segment_label }}. Temadag "{{ visit.topic_name }}", {{ visit.location }}. {{ visit.students_nr|default('???') }} elever.{{ '  ' }}Specialkost: {{ visit.diet|default('???') }}{% endfor -%}{%- endfor -%}{%- endfor -%}</textarea></div>{% endblock %}